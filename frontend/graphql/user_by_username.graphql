query UserByUsername(
  $username: String!
  ) {
  userByUsername(
      username: $username
  ) {
    id
    email
    username
    nickname
    blogName
    website
    banned
    createdAt
    updatedAt
  }
}
