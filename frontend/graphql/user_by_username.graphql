query UserByUsername(
  $username: String!
  ) {
  userByUsername(
      username: $username
  ) {
    id
    email
    username
    nickname
    blogName
    website
    banned
    createdAt
    updatedAt

    articles {
      id
      username
      subject
      slug
      content
      createdAt
      updatedAt
      published
      top
      recommended
    }
  }
}
