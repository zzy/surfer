mutation UserRegister(
  $email: String!
  $username: String!
  $nickname: String!
  $cred: String!
  $blogName: String!
  $website: String!
  $createdAt: DateTime!
  $updatedAt: DateTime!
  ) {
  userRegister(
    userNew: {
      email: $email
      username: $username
      nickname: $nickname
      cred: $cred
      blogName: $blogName
      website: $website
      createdAt: $createdAt
      updatedAt: $updatedAt
    }
  ) {
    id
    email
    username
    nickname
    blogName
    website
    createdAt
    updatedAt
    banned
  }
}
